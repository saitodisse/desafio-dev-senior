'use strict';

MapApp.module('Models', function (Models, App, Backbone) {

    // ---------------
    // VehicleInfo Model
    // ---------------
    Models.RouteResultModel = Backbone.Model.extend({
        defaults: {
        },
    });

});
