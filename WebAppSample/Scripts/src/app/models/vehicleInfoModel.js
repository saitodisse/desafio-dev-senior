'use strict';

MapApp.module('Models', function (Models, App, Backbone) {

    // ---------------
    // VehicleInfo Model
    // ---------------
    Models.VehicleInfoModel = Backbone.Model.extend({
        defaults: {
        },
    });

});
